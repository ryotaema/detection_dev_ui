# =============================================================================
# ui — 画面（タブ・ウィジェット）
#
#   各タブは render_xxx() として独立している。main.py はそれを呼ぶだけ。
#   ロジックは core/ にあり、ui/ からは `from core import *` で使う。
# =============================================================================
